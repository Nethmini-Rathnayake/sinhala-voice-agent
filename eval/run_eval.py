"""Batch evaluation over ``eval/utterances/``.

Runs the pipeline on every recorded test WAV (Sinhala, English, mixed) and
writes a CSV of transcripts, replies and per-stage latencies.

CLI: ``python -m eval.run_eval``
"""
